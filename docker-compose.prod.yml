services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile.prod
    container_name: task-activity-backend-prod
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      NODE_ENV: production
    restart: unless-stopped
